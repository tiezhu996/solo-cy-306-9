<!-- 改编自：综合实用主题项目提示词/cy-306/cy-306.md；后端由 Django (Python) 改造为 Go 1.22 + Gin + GORM -->
# cy-306 活动报名通

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**活动报名通（gbevent）** 是一个支持活动发布、在线报名、名额管理、签到统计、活动日历视图以及评论收藏与消息通知的活动管理系统，适用于讲座、培训、聚会、比赛等各类线下或线上活动场景。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, username, password_hash, nickname, avatar, role（user/organizer/admin）, email, phone, created_at
  - 关键关系：可创建多个 Activity、Registration、Comment、Favorite 和 Notification
  - 业务动作：注册/登录、修改资料、查看个人中心
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.vue**

- **Activity（活动）**
  - 字段：id, title, description, cover_image, activity_type（lecture/training/party/competition）, start_time, end_time, location, capacity, signup_deadline, status（draft/published/ended）, organizer_id, created_at
  - 关键关系：隶属于发布者（User.organizer），被多个 Registration、Comment、Favorite 引用
  - 业务动作：创建、编辑、下架、日历视图、名额校验
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **Registration（报名）**
  - 字段：id, activity_id, user_id, name, phone, remark, voucher_no, status（registered/cancelled/checked_in）, review_status（pending/approved/rejected）, created_at
  - 关键关系：隶属于用户，引用 Activity，可生成 CheckInRecord
  - 业务动作：在线报名、取消、审核、名单导出、线下补录
  - **此实体必须贯穿全栈**

- **CheckInRecord（签到记录）**
  - 字段：id, registration_id, activity_id, check_in_method（voucher/scan）, check_in_time, operator_id, created_at
  - 关键关系：引用 Registration 和 Activity
  - 业务动作：凭证号签到、扫码签到、签到率统计
  - **此实体必须贯穿全栈**

- **Comment（活动评论）**
  - 字段：id, activity_id, user_id, rating（1-5）, content, created_at
  - 关键关系：引用 Activity 和 User
  - 业务动作：发表评论、评分、评论列表与平均分
  - **此实体必须贯穿全栈**

- **Favorite（收藏）**
  - 字段：id, user_id, activity_id, created_at
  - 关键关系：引用 User 和 Activity
  - 业务动作：收藏活动、取消收藏、查看收藏列表
  - **此实体必须贯穿全栈**

- **Notification（消息通知）**
  - 字段：id, user_id, notification_type（signup_success/reminder/review_result/checkin_success）, title, content, is_read, created_at
  - 关键关系：隶属于用户
  - 业务动作：报名成功、活动前 24 小时提醒、审核结果、签到成功通知
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/calendar（活动日历首页）**
  - 消费实体：Activity
  - 关键交互：月历视图展示活动分布、日期格子显示活动数量、点击日期展开当天活动、列表/日历切换
  - 共享组件：`<ActivityCalendar>`、`<ActivityCard>`

- **/activities（活动列表）**
  - 消费实体：Activity
  - 关键交互：进行中活动卡片展示、类型/时间筛选、查看详情
  - 共享组件：`<ActivityCard>`（与 /calendar 共用）、`<ActivityFilter>`

- **/activities/:id（活动详情）**
  - 消费实体：Activity、Registration、Comment、Favorite
  - 关键交互：活动详情、报名表单、评论列表与评分、收藏
  - 共享组件：`<SignupForm>`、`<CommentList>`、`<RatingStars>`

- **/organizer/activities（活动发布管理）**
  - 消费实体：Activity、User
  - 关键交互：创建活动、编辑、下架、状态切换
  - 共享组件：`<ActivityForm>`、`<DataTable>`

- **/organizer/registrations（报名名单与签到）**
  - 消费实体：Registration、CheckInRecord、Activity
  - 关键交互：名单筛选、手动审核、导出 Excel、凭证签到、扫码签到、签到率统计
  - 共享组件：`<RegistrationTable>`、`<CheckInQrCode>`、`<CheckInPanel>`

- **/profile（个人中心）**
  - 消费实体：Registration、Favorite、Comment、Notification、User
  - 关键交互：我的报名、收藏列表、我的评论、消息通知
  - 共享组件：`<MyRegistrations>`、`<FavoriteList>`、`<NotificationList>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中发布者/管理员专属接口需 `AuthRequired()` 和 `RequireRole()`
    - 前端：`router/guards.ts`、`components/common/RoleGuard.vue`、`stores/authStore.ts`

- **接口限流**
  - 触达文件层：
    - 后端：`internal/middleware/rate_limiter.go`
    - 路由：登录、报名、签到等接口启用限流
    - 配置：`internal/config/config.go`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器

- **文件上传**
  - 触达文件层：
    - 后端：`internal/handler/upload_handler.go`、`internal/util/file_upload.go`
    - 前端：`components/common/ImageUploader.vue`（活动封面图上传）
    - 配置：`internal/config/config.go`（上传大小与类型限制）

### 5. 共享枚举/常量（≥ 2 组）

- **ActivityStatus（活动状态）**
  - 值：DRAFT = 'draft', PUBLISHED = 'published', ENDED = 'ended'
  - 前端定义位置：`frontend/src/constants/activity.ts`
  - 后端定义位置：`backend/internal/constants/activity.go`
  - 出现位置：Activity 模型、ActivityService、活动列表、活动发布管理、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **RegistrationStatus（报名状态）**
  - 值：REGISTERED = 'registered', CANCELLED = 'cancelled', CHECKED_IN = 'checked_in'
  - 前端定义位置：`frontend/src/constants/registration.ts`
  - 后端定义位置：`backend/internal/constants/registration.go`
  - 出现位置：Registration 模型、RegistrationService、RegistrationTable、签到面板、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **ActivityType（活动类型）**
  - 值：LECTURE = 'lecture', TRAINING = 'training', PARTY = 'party', COMPETITION = 'competition'
  - 前端定义位置：`frontend/src/constants/activity.ts`
  - 后端定义位置：`backend/internal/constants/activity.go`
  - 出现位置：Activity 模型、ActivityService、ActivityFilter、活动列表、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<ActivityCard>`：被 /calendar 和 /activities 共用
- `<ActivityCalendar>`：被 /calendar 使用
- `<ActivityFilter>`：被 /activities 使用
- `<SignupForm>`：被 /activities/:id 使用
- `<CommentList>`：被 /activities/:id 使用
- `<RatingStars>`：被 /activities/:id 使用
- `<ActivityForm>`：被 /organizer/activities 使用
- `<RegistrationTable>`：被 /organizer/registrations 使用
- `<CheckInQrCode>`：被 /organizer/registrations 使用
- `<CheckInPanel>`：被 /organizer/registrations 使用
- `<MyRegistrations>`：被 /profile 使用
- `<FavoriteList>`：被 /profile 使用
- `<NotificationList>`：被 /profile 使用
- `<ImageUploader>`：被 /organizer/activities 使用
- `<EmptyState>`：被多个列表页共用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录态与角色判断
- `useActivityStats()`：活动报名与签到统计
- `useCheckIn()`：凭证/扫码签到逻辑
- `utils/dateFormat.ts`：日期时间格式化
- `utils/voucherGenerator.ts`：报名凭证号生成与校验
- `utils/request.ts`：Axios 请求封装与错误拦截

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `rate_limiter.go`：接口限流
- `error_handler.go`：统一错误响应格式
- `audit_log.go`：操作审计日志
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbevent`
- **前端端口**：18506
- **后端端口**：19506
- **数据库端口**：57506
- **前端框架**：Vue 3 + TypeScript
- **UI 库**：Element Plus
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, activity.ts, registration.ts, checkIn.ts, comment.ts, favorite.ts, notification.ts
├── stores/           # authStore.ts, userStore.ts, activityStore.ts, registrationStore.ts, commentStore.ts, favoriteStore.ts, notificationStore.ts
├── types/            # 共享类型定义
├── components/common/# ActivityCard, ActivityCalendar, ActivityFilter, SignupForm, CommentList, RatingStars, ActivityForm, RegistrationTable, CheckInQrCode, CheckInPanel, MyRegistrations, FavoriteList, NotificationList, ImageUploader, EmptyState, RoleGuard
├── hooks/            # useAuth.ts, useActivityStats.ts, useCheckIn.ts
├── pages/            # Calendar, Activities, ActivityDetail, OrganizerActivities, OrganizerRegistrations, Profile
├── router/           # index.ts + guards.ts
├── utils/            # dateFormat.ts, voucherGenerator.ts, request.ts
└── constants/        # activity.ts, registration.ts, notification.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、限流、端口、上传限制等）
    ├── model/                   # 按实体分文件：user.go, activity.go, registration.go, check_in_record.go, comment.go, favorite.go, notification.go
    ├── repository/              # 按实体分文件：user_repository.go, activity_repository.go, registration_repository.go, check_in_record_repository.go, comment_repository.go, favorite_repository.go, notification_repository.go
    ├── service/                 # 按实体分文件：user_service.go, activity_service.go, registration_service.go, check_in_record_service.go, comment_service.go, favorite_service.go, notification_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, activity_handler.go, registration_handler.go, check_in_record_handler.go, comment_handler.go, favorite_handler.go, notification_handler.go, upload_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, activities.go, registrations.go, check_ins.go, comments.go, favorites.go, notifications.go
    ├── middleware/              # auth.go, rbac.go, rate_limiter.go, error_handler.go, audit_log.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # activity.go, registration.go, notification.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, voucher_generator.go, file_upload.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `REGISTRATION_CREATE_SUCCESS`、`CHECKIN_SCAN_FAILED`
   - 任何业务字段变更（如 Registration 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"Registration[id=123] approve failed: organizer not match"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、名额格式化、状态文本、活动类型文本、报名状态文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **报名状态机必须跨多处定义**
   - RegistrationStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - ActivityStatus、RegistrationStatus 和 ActivityType 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 Activity 新增一个 'online' 活动形式"，必须触达：GORM AutoMigrate、Activity 模型、前后端 constants、ActivityService、ActivityCard、ActivityFilter、formatters、日志模板、错误码、日历视图渲染逻辑等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbevent`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbevent`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbevent}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `${FRONTEND_PORT:-18506}:80`，后端 `${BACKEND_PORT:-19506}:8080`，数据库 `${DB_PORT:-57506}:3306`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbevent`
- 数据库连接相关：`DB_NAME=gbevent_db`、`DB_USER=gbevent_user`、`DB_PASSWORD=gbevent_pwd`、`DB_ROOT_PASSWORD=gbevent_root`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=18506`、`BACKEND_PORT=19506`、`DB_PORT=57506`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 ActivityStatus、RegistrationStatus 和 ActivityType 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18506` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<ActivityCard>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
